name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e  # v7.0.0
        with:
          go-version: "1.26.6"
      - run: go vet ./...
      - name: assert-ran guard self-test (H7 anti-false-green guard-of-the-guard)
        run: bash scripts/ci/assert-ran_test.sh
      - name: govulncheck gate self-test
        run: bash scripts/ci/govulncheck-gate_test.sh
      - run: go test -race -coverprofile=coverage.out ./...
      - name: Check coverage threshold
        run: |
          COVERAGE=$(go tool cover -func=coverage.out | tail -1 | awk '{print $3}' | tr -d '%')
          echo "Total coverage: ${COVERAGE}%"
          if (( $(echo "$COVERAGE < 30" | bc -l) )); then
            echo "::error::Coverage ${COVERAGE}% is below 30% threshold"
            exit 1
          fi
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: coverage
          path: coverage.out

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e  # v7.0.0
        with:
          go-version: "1.26.6"
      - uses: golangci/golangci-lint-action@ba0d7d2ec06a0ea1cb5fa41b2e4a3ab91d21278a  # v9.3.0
        with:
          version: v2.12.2
      - name: gofmt (changed Go files)
        run: |
          base="${{ github.event.pull_request.base.sha }}"
          if [ -z "$base" ]; then base="${{ github.event.before }}"; fi
          git rev-parse --verify --quiet "$base^{commit}" >/dev/null 2>&1 || base="$(git rev-parse origin/main 2>/dev/null || echo HEAD~1)"
          mapfile -t files < <(git diff --name-only --diff-filter=d "$base" HEAD -- '*.go')
          if [ "${#files[@]}" -gt 0 ]; then
            drift=$(gofmt -l "${files[@]}")
            if [ -n "$drift" ]; then
              echo "::error::gofmt drift in changed files:"; echo "$drift"; exit 1
            fi
          fi

  mutation:
    name: Output mutation harness (hard gate)
    runs-on: ubuntu-latest
    # The harness re-runs the whole corpus once per mutant — measured at about
    # two minutes in a 10-core container. This ceiling sits ABOVE the target's
    # own `-timeout 20m` on purpose: a genuine hang should produce Go's stack
    # dump naming the test, not a bare cancellation.
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e  # v7.0.0
        with:
          go-version: "1.26.6"
      # The lint job above never sees internal/output/mutation_test.go:
      # golangci-lint does not compile a //go:build-tagged file, so ~700 lines
      # would ship unlinted without this step.
      #
      # The linter is installed and invoked directly, at the version the lint
      # job pins, rather than through golangci-lint-action with
      # `args: --build-tags mutation`. That is deliberate: an `args:` the
      # action did not forward would leave a GREEN step over an unlinted file,
      # so the failure would be silent rather than loud. Running
      # `make lint-mutation` takes the forwarding out of the chain instead of
      # trusting it, and makes CI execute the same string a developer runs
      # locally. The flag itself is not taken on trust either: with a lint
      # error planted in the tagged file, plain `golangci-lint run` stays at
      # 0 issues while `make lint-mutation` reports it and exits non-zero.
      #
      # WHAT THIS COSTS, since the paragraph above only names what it buys:
      # `go install` resolves the linter from the module proxy at job time, so
      # this step is the one place in this workflow not pinned to a commit SHA
      # — measured, 17 of 17 `uses:` here are — and it trades the action's
      # own download caching for a build on every run, 26s in a 10-core
      # container with a cold build cache. The version is pinned exactly and
      # the module checksum database still authenticates the download, so what
      # is given up is the stronger pin and the cache, not the integrity
      # check.
      - name: Lint what the build tag hides from the lint job
        run: |
          go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.12.2
          export PATH="$(go env GOPATH)/bin:$PATH"
          make lint-mutation
      - name: Plant every defect switch and require each one dies
        run: make test-mutation

  datapath:
    runs-on: ubuntu-latest
    # Hard ceiling: every docker command in the gate is also timeout-wrapped, but
    # this guarantees the job can never hang the workflow (a prior diagnostic did).
    timeout-minutes: 8
    permissions:
      contents: read
    env:
      # Full commit SHA of dotfiles main after PR rtxnik/dotfiles#20 merged
      # (pins the proxy base image FROM to a manifest-list digest; D6-06 — #20
      # moved the tag note off the FROM line so `docker build` parses it). The
      # recipe at this SHA also carries the H4 entrypoint + H10 dig changes.
      # This ref MUST equal the ws-embedded proxy recipe pin
      # (internal/proxyrecipe/recipe.lock -> dotfiles_ref): the datapath gate
      # builds the staged recipe directly and its comment relies on that equality.
      # actions/checkout requires
      # a full 40-char SHA or a branch/tag name — a short SHA is treated as a
      # branch pattern and fails to resolve. Pin a SHA (not `main`) so the gate is
      # reproducible; bump it when the proxy recipe changes again.
      DOTFILES_REF: "cf4ca76ec7c4fbd120673db76ac1e121973ea099"
      # WS_TEST_URI drives strict mode (real profile + exit-IP assertion).
      # If the WS_TEST_ENDPOINT secret is unset the gate runs in flow-only mode
      # (TPROXY preconditions + forwarding leg asserted; exit-IP comparison skipped).
      WS_TEST_URI: ${{ secrets.WS_TEST_ENDPOINT }}
      # Deterministic tag so the gate builds it and the H6/H7 steps reuse the
      # same image from the runner's docker daemon (no rebuild, one source of truth).
      WS_PROXY_IMAGE: ws-proxy:gate
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e  # v7.0.0
        with:
          go-version: "1.26.6"
      - name: Preflight — load TPROXY kernel modules
        run: |
          sudo modprobe xt_TPROXY xt_socket || {
            echo "::error::runner kernel lacks xt_TPROXY/xt_socket; datapath gate cannot run"
            exit 1
          }
      - name: Checkout dotfiles proxy recipe (pinned)
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          repository: rtxnik/dotfiles
          ref: ${{ env.DOTFILES_REF }}
          path: _dotfiles
          persist-credentials: false
      - name: Build ws
        run: make build
      - name: Stage proxy recipe where ws expects it
        run: |
          mkdir -p "$HOME/.config/workspaces/profiles"
          cp -r _dotfiles/dot_config/workspaces/profiles/proxy "$HOME/.config/workspaces/profiles/proxy"
      - name: Verify staged proxy recipe against embedded pin
        run: WS_STAGED_PROFILES_DIR="$HOME/.config/workspaces/profiles" go test ./internal/proxyrecipe -run TestVerifyStagedRecipe -v
      - name: Run red-line datapath gate (T2-T5 + T13)
        run: sudo -E WS_BIN="$PWD/ws" bash scripts/ci/datapath-gate.sh

      - name: H6 — xray -test over goldens + VLESS matrix (semantic golden gate)
        run: |
          set -o pipefail
          go test -tags docker_e2e ./cmd/ -run TestXrayValidatesConfigs -v 2>&1 | tee /tmp/h6.log
          bash scripts/ci/assert-ran.sh TestXrayValidatesConfigs < /tmp/h6.log

      - name: Restart dev-proxy for H7 (T13 left it stopped)
        run: |
          # T13 disabled the restart policy and killed xray (PID 1), leaving
          # dev-proxy stopped. Restore the restart policy and bring it back up
          # so requireDevProxy (docker ps -q -f name=dev-proxy) sees it running.
          docker update --restart=unless-stopped dev-proxy
          docker start dev-proxy
          # Wait until the container is healthy / running before proceeding.
          for i in $(seq 1 15); do
            status="$(docker inspect -f '{{.State.Status}}' dev-proxy 2>/dev/null || echo missing)"
            if [ "$status" = "running" ]; then
              echo "dev-proxy is running"
              break
            fi
            echo "waiting for dev-proxy ($status) ... $i/15"
            sleep 1
          done

      - name: H7 — proxy profile-lifecycle integration (dev-proxy is up)
        # Only TestIntegration_Cycle runs in CI: it is t.Setenv-isolated to a
        # tmpdir (it never touches the real ~/.config/xray) and exercises the
        # add/swap/read/rm pipeline against the live dev-proxy. The other two
        # `integration` tests are intentionally NOT run here:
        #   - TestProfileLifecycleE2E is the operator-machine checkpoint (its own
        #     doc): it mutates the real ~/.config/xray/profiles, which the sudo'd
        #     datapath gate root-owns, so a non-sudo runner write is EPERM.
        #   - TestExistingStateDiscovery skips without an operator profile set.
        # `make test-integration-proxy` still runs all three for local operators.
        run: |
          set -o pipefail
          go test -tags integration ./internal/xray/ -run 'TestIntegration_Cycle' -v 2>&1 | tee /tmp/h7.log
          bash scripts/ci/assert-ran.sh TestIntegration_Cycle < /tmp/h7.log

      - name: H7 (strict) — full-tunnel e2e (only when WS_TEST_URI is set)
        if: ${{ env.WS_TEST_URI != '' }}
        run: |
          set -o pipefail
          make test-e2e 2>&1 | tee /tmp/e2e.log
          bash scripts/ci/assert-ran.sh TestProxyE2E < /tmp/e2e.log

  snapshot-gate:
    name: Release snapshot + signature/SBOM gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e  # v7.0.0
        with:
          go-version: "1.26.6"
          cache: false
      - name: Install minisign
        run: |
          sudo apt-get update
          sudo apt-get install -y minisign
      - name: Install syft
        uses: anchore/sbom-action/download-syft@3ad7283483fc7af8ff2b4ea19663c2d5ca935e26  # v0.24.2
      - name: Generate ephemeral minisign key
        run: |
          set -euo pipefail
          key_dir="$(mktemp -d)"
          printf 'ci-snapshot\nci-snapshot\n' | minisign -G \
            -p "${key_dir}/minisign.pub" \
            -s "${key_dir}/minisign.key"
          {
            echo "MINISIGN_SECRET_KEY_FILE=${key_dir}/minisign.key"
            echo "MINISIGN_PUBLIC_KEY_FILE=${key_dir}/minisign.pub"
            echo "MINISIGN_PASSWORD=ci-snapshot"
          } >> "$GITHUB_ENV"
      - name: Build snapshot release with signing + SBOMs
        uses: goreleaser/goreleaser-action@f06c13b6b1a9625abc9e6e439d9c05a8f2190e94  # v7.2.3
        with:
          distribution: goreleaser
          version: "~> v2"
          args: release --snapshot --clean
        env:
          MINISIGN_SECRET_KEY_FILE: ${{ env.MINISIGN_SECRET_KEY_FILE }}
          MINISIGN_PASSWORD: ${{ env.MINISIGN_PASSWORD }}
      - name: Verify signature + SBOM artifacts
        run: |
          set -euo pipefail
          test -f dist/checksums.txt
          test -f dist/checksums.txt.minisig
          minisign -V -p "${MINISIGN_PUBLIC_KEY_FILE}" -m dist/checksums.txt -x dist/checksums.txt.minisig
          ls dist/*.sbom.json >/dev/null 2>&1 || { echo "::error::no SBOM artifacts produced"; exit 1; }
      - name: Exercise the verified installer
        run: sh scripts/install_test.sh

  govulncheck:
    name: govulncheck (module-bound allowlist gate)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e  # v7.0.0
        with:
          go-version: "1.26.6"
      - name: Run govulncheck gate
        run: bash scripts/ci/govulncheck-gate.sh
